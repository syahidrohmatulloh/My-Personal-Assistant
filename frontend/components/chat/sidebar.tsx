"use client"; import Link from "next/link";
import { useRouter, useParams } from "next/navigation";
import { ChevronDown, Heart, LogOut, Menu, MessageSquare, MoreHorizontal, Pencil, Plus, RefreshCw, Settings, Sparkles, Target, Trash2, User, Users, X,
} from "lucide-react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { useEffect, useMemo, useRef, useState } from "react";
import { type Conversation, type StyleProfile, createConversation, deleteConversation, getTodaysJournal, getIdentity, listConversations, listStyleProfiles, regenerateConversationTitle, renameConversation, setConversationStyle,
} from "@/lib/api";
import { createClient } from "@/lib/supabase/client";
import { Skeleton } from "@/components/ui/skeleton";
import { cn } from "@/lib/utils"; // Group conversations by relative time, like ChatGPT / Claude.ai
type ConvoGroup = { label: string; conversations: Conversation[] }; function groupConversations(convos: Conversation[]): ConvoGroup[] { const now = Date.now(); const ONE_DAY = 86_400_000; const buckets: { [k: string]: Conversation[] } = { Today: [], Yesterday: [], "Previous 7 days": [], "Previous 30 days": [], Older: [], }; for (const c of convos) { const age = now - new Date(c.updated_at).getTime(); if (age < ONE_DAY) buckets.Today.push(c); else if (age < 2 * ONE_DAY) buckets.Yesterday.push(c); else if (age < 7 * ONE_DAY) buckets["Previous 7 days"].push(c); else if (age < 30 * ONE_DAY) buckets["Previous 30 days"].push(c); else buckets.Older.push(c); } return Object.entries(buckets) .filter(([, items]) => items.length > 0) .map(([label, conversations]) => ({ label, conversations }));
} export function Sidebar({
  initialConversations = [],
  initialJournaled = false,
}: {
  initialConversations?: Conversation[];
  initialJournaled?: boolean;
}) { const router = useRouter(); const params = useParams<{ id?: string }>(); const activeId = params?.id; const qc = useQueryClient(); const [open, setOpen] = useState(false); const { data: identity } = useQuery({ queryKey: ["identity"], queryFn: getIdentity, staleTime: 5 * 60 * 1000, gcTime: 30 * 60 * 1000, refetchOnMount: false, refetchOnWindowFocus: false, }); const assistantName = typeof identity?.profile?.assistant_name === "string" && identity.profile.assistant_name.trim().length > 0 ? identity.profile.assistant_name.trim() : "Aliyya"; const { data: conversations = [], isLoading } = useQuery({ queryKey: ["conversations"], queryFn: listConversations, staleTime: 5 * 60 * 1000, gcTime: 30 * 60 * 1000, refetchOnMount: false, refetchOnWindowFocus: false, refetchOnReconnect: false, initialData: initialConversations, }); const { data: today } = useQuery({ queryKey: ["journal", "today"], queryFn: getTodaysJournal, initialData: {
      entry: initialJournaled
        ? {
            id: "hydrated",
            mood: null,
            energy: null,
            stress: null,
            note: null,
            observed_at: new Date().toISOString(),
          }
        : null,
    }, }); const journaledToday = today?.entry != null;

  const { data: memoryHealthStatus } = useQuery({
    queryKey: ["memory-health-status-live"],
    queryFn: async () => {
      const schedulerRes = await fetch("/api/memory-review/quality/scheduler/status", {
        cache: "no-store",
      })

      if (schedulerRes.ok) {
        const schedulerJson = (await schedulerRes.json()) as {
          user_summary?: {
            needs_review?: number
          } | null
        }

        const schedulerCount = schedulerJson.user_summary?.needs_review
        if (typeof schedulerCount === "number") {
          return { needs_review: schedulerCount, source: "scheduler" as const }
        }
      }

      const liveRes = await fetch("/api/memory-review/quality", {
        cache: "no-store",
      })

      if (!liveRes.ok) return { needs_review: 0, source: "none" as const }

      const liveJson = (await liveRes.json()) as {
        summary?: {
          needs_review?: number
        }
      }

      return {
        needs_review: Number(liveJson.summary?.needs_review || 0),
        source: "live" as const,
      }
    },
    staleTime: 60_000,
    gcTime: 5 * 60 * 1000,
    refetchOnWindowFocus: true,
  });

  const memoryNeedsReview = Math.max(
    0,
    Number(memoryHealthStatus?.needs_review || 0),
  ); const { data: styleProfiles = [] } = useQuery({ queryKey: ["style-profiles"], queryFn: listStyleProfiles, staleTime: 60_000, }); const groups = useMemo(() => groupConversations(conversations), [conversations]); useEffect(() => { setOpen(false); }, [activeId]); useEffect(() => { if (open) { const prev = document.body.style.overflow; document.body.style.overflow = "hidden"; return () => { document.body.style.overflow = prev; }; } }, [open]); const createMut = useMutation({ mutationFn: (styleProfileId: string | null = null) => createConversation("New chat", styleProfileId), onMutate: async () => { const optimistic: Conversation = { id: `temp-${Date.now()}`, title: "New chat", created_at: new Date().toISOString(), updated_at: new Date().toISOString(), }; await qc.cancelQueries({ queryKey: ["conversations"] }); qc.setQueryData<Conversation[]>(["conversations"], (old = []) => [ optimistic, ...old, ]); return { optimistic }; }, onSuccess: (real, _vars, ctx) => { qc.setQueryData<Conversation[]>(["conversations"], (old = []) => old.map((c) => (c.id === ctx?.optimistic.id ? real : c)), ); setOpen(false); router.push(`/chat/${real.id}`); }, onError: (_e, _v, ctx) => { qc.setQueryData<Conversation[]>(["conversations"], (old = []) => old.filter((c) => c.id !== ctx?.optimistic.id), ); }, }); const deleteMut = useMutation({ mutationFn: (id: string) => deleteConversation(id), onMutate: async (id) => { await qc.cancelQueries({ queryKey: ["conversations"] }); const prev = qc.getQueryData<Conversation[]>(["conversations"]); qc.setQueryData<Conversation[]>(["conversations"], (old = []) => old.filter((c) => c.id !== id), ); return { prev }; }, onError: (_e, _id, ctx) => { if (ctx?.prev) qc.setQueryData(["conversations"], ctx.prev); }, }); function handleDelete(id: string) { if (!confirm("Delete this conversation?")) return; deleteMut.mutate(id); if (activeId === id) router.push("/chat"); } async function handleSignOut() { const supabase = createClient(); await supabase.auth.signOut(); router.push("/login"); router.refresh(); } const sidebarBody = ( <> {/* Brand */} <div className="shrink-0 px-4 pt-4 pb-3 flex items-center gap-2"> <div className="h-7 w-7 rounded-lg bg-accent grid place-items-center shadow-sm"> <Sparkles className="h-3.5 w-3.5 text-on-accent" strokeWidth={2.5} /> </div> <span className="text-sm font-semibold text-fg tracking-tighter"> {assistantName} </span> <button onClick={() => setOpen(false)} className="ml-auto md:hidden h-9 w-9 grid place-items-center rounded-lg text-fg-muted " aria-label="Close menu" > <X className="h-5 w-5" /> </button> </div> {/* New chat */} <div className="shrink-0 px-3 pb-3"> <div className="flex gap-1.5"> <button onClick={() => createMut.mutate(null)} disabled={createMut.isPending} className="flex-1 flex items-center gap-2 rounded-xl bg-accent text-on-accent px-3 py-2.5 text-sm font-medium hover:bg-accent-hover transition-all hover:shadow-lg hover:shadow-accent/20 active:scale-[0.98] disabled:opacity-60" > <Plus className="h-4 w-4" strokeWidth={2.5} /> New chat </button> {styleProfiles.length > 0 && ( <NewChatStyleDropdown profiles={styleProfiles} onPick={(profileId) => createMut.mutate(profileId)} disabled={createMut.isPending} /> )} </div> </div> {/* History header */} {!isLoading && conversations.length > 0 && ( <div className="shrink-0 px-4 pb-1.5"> <p className="text-[10px] font-semibold uppercase tracking-wider text-fg-subtle"> Chat History </p> </div> )} {/* Conversations — flex-1 + min-h-0 lets this scroll independently */} <div className="flex-1 min-h-0 overflow-y-auto scroll-smooth-mobile px-2"> {isLoading ? ( <> <Skeleton className="h-8 mx-1 mb-1" /> <Skeleton className="h-8 mx-1 mb-1 w-3/4" /> <Skeleton className="h-8 mx-1 mb-1 w-2/3" /> </> ) : conversations.length === 0 ? ( <div className="px-2 py-3 text-center"> <MessageSquare className="h-5 w-5 text-fg-subtle mx-auto mb-1.5 opacity-50" /> <p className="text-xs text-fg-subtle">No conversations yet</p> </div> ) : ( groups.map((g) => ( <div key={g.label} className="mb-3"> {/* Section header — clearly heavier and slightly larger than chat titles below. Title rows use text-sm font-normal (14px), this is text-[13px] font-semibold which reads as the heavier element due to weight + the breathing spacing above. Sentence-case (Today, not TODAY) — calmer than uppercase. */} <p className="px-2.5 pt-3 pb-1.5 text-[13px] font-semibold text-fg"> {g.label} </p> {g.conversations.map((c) => ( <ConversationRow key={c.id} conversation={c} active={activeId === c.id} onDelete={() => handleDelete(c.id)} styleProfiles={styleProfiles} /> ))} </div> )) )} </div> {/* Footer */} <div className="shrink-0 px-3 py-3 border-t border-border space-y-0.5 pb-safe"> <NavLink href="/journal" icon={<Heart className="h-4 w-4" />} label="Journal" showDot={today != null && !journaledToday} /> <NavLink href="/goals" icon={<Target className="h-4 w-4" />} label="Goals" /> <NavLink href="/people" icon={<Users className="h-4 w-4" />} label="People" /> <NavLink href="/identity" icon={<User className="h-4 w-4" />} label="Identity" /> <NavLink href="/memories" icon={<Sparkles className="h-4 w-4" />} label="Memories" badge={memoryNeedsReview} /> <NavLink href="/settings" icon={<Settings className="h-4 w-4" />} label="Settings" /> <button onClick={handleSignOut} className="group w-full flex items-center gap-2.5 rounded-lg px-2.5 py-2 text-sm text-fg-soft dark:text-zinc-200 active:bg-fg/10 transition-colors hover:bg-slate-900/[0.06] hover:text-slate-950 dark:hover:bg-white/[0.14] dark:hover:text-white" > <LogOut className="h-4 w-4 text-fg-muted dark:text-zinc-300 transition-colors group-hover:text-slate-950 dark:group-hover:text-white" /> Sign out </button> </div> </> ); return ( <> {/* Mobile topbar */} <div className="md:hidden fixed top-0 left-0 right-0 z-30 glass border-b border-border pt-safe"> <div className="flex items-center gap-2 px-3 py-2"> <button onClick={() => setOpen(true)} className="h-10 w-10 grid place-items-center rounded-lg text-fg active:bg-fg/10" aria-label="Open menu" > <Menu className="h-5 w-5" /> </button> <div className="flex items-center gap-2"> <div className="h-6 w-6 rounded-md bg-accent grid place-items-center"> <Sparkles className="h-3 w-3 text-on-accent" strokeWidth={2.5} /> </div> <span className="text-sm font-semibold text-fg tracking-tighter"> {assistantName} </span> </div> </div> </div> {/* Mobile drawer */} {open && ( <> <div className="md:hidden fixed inset-0 z-40 bg-black/40 drawer-backdrop-enter" onClick={() => setOpen(false)} aria-hidden /> <aside className="app-sidebar md:hidden glass-strong fixed left-0 top-0 bottom-0 z-50 w-[85vw] max-w-[300px] flex flex-col drawer-enter" role="dialog" aria-label="Navigation" > {sidebarBody} </aside> </> )} {/* Desktop sidebar — explicit height accounting for m-2 (= 0.5rem each side) */} <aside className="app-sidebar hidden md:flex glass-strong w-64 shrink-0 flex-col h-[calc(100dvh-1rem)] sticky top-2 m-2 mr-0 rounded-2xl overflow-hidden"> {sidebarBody} </aside> </> );
} // =============================================================================
// Individual conversation row — owns its rename / menu state.
// =============================================================================
function ConversationRow({ conversation, active, onDelete, styleProfiles,
}: { conversation: Conversation; active: boolean; onDelete: () => void; styleProfiles: StyleProfile[];
}) { const qc = useQueryClient(); const [editing, setEditing] = useState(false); const [draft, setDraft] = useState(conversation.title || ""); const [menuOpen, setMenuOpen] = useState(false); const [stylePickerOpen, setStylePickerOpen] = useState(false); const inputRef = useRef<HTMLInputElement>(null); useEffect(() => { if (editing) inputRef.current?.select(); }, [editing]); const renameMut = useMutation({ mutationFn: (title: string) => renameConversation(conversation.id, title), onMutate: async (title) => { await qc.cancelQueries({ queryKey: ["conversations"] }); const prev = qc.getQueryData<Conversation[]>(["conversations"]); qc.setQueryData<Conversation[]>(["conversations"], (old = []) => old.map((c) => (c.id === conversation.id ? { ...c, title } : c)), ); return { prev }; }, onError: (_e, _v, ctx) => { if (ctx?.prev) qc.setQueryData(["conversations"], ctx.prev); }, }); const regenMut = useMutation({ mutationFn: () => regenerateConversationTitle(conversation.id), onSuccess: (real) => { qc.setQueryData<Conversation[]>(["conversations"], (old = []) => old.map((c) => (c.id === conversation.id ? real : c)), ); }, }); const styleMut = useMutation({ mutationFn: (profileId: string | null) => setConversationStyle(conversation.id, profileId), onMutate: async (profileId) => { await qc.cancelQueries({ queryKey: ["conversations"] }); const prev = qc.getQueryData<Conversation[]>(["conversations"]); qc.setQueryData<Conversation[]>(["conversations"], (old = []) => old.map((c) => c.id === conversation.id ? { ...c, style_profile_id: profileId } : c, ), ); return { prev }; }, onError: (_e, _v, ctx) => { if (ctx?.prev) qc.setQueryData(["conversations"], ctx.prev); }, }); function saveRename() { const t = draft.trim(); if (t && t !== conversation.title) { renameMut.mutate(t); } setEditing(false); } function cancelRename() { setDraft(conversation.title || ""); setEditing(false); } function onKeyDown(e: React.KeyboardEvent<HTMLInputElement>) { if (e.key === "Enter") { e.preventDefault(); saveRename(); } else if (e.key === "Escape") { e.preventDefault(); cancelRename(); } } const isProtectedConversation =
    (conversation.title || "").startsWith("Main Chat -") ||
    (conversation.title || "") === "Briefings";

  if (editing) { return ( <div className={cn( "rounded-lg px-2.5 py-1.5 mb-0.5", active ? "bg-accent-soft" : "bg-fg/5", )} > <input ref={inputRef} value={draft} onChange={(e) => setDraft(e.target.value)} onKeyDown={onKeyDown} onBlur={saveRename} maxLength={120} className="w-full bg-transparent text-sm text-fg outline-none border-b border-accent/40 focus:border-accent" /> </div> ); } return ( <div className="relative"> <Link href={`/chat/${conversation.id}`} className={cn( "group flex items-center justify-between gap-2 rounded-lg px-2.5 py-2 text-sm mb-0.5 transition-all", active ? "bg-accent-soft text-fg font-medium" : "text-fg-soft dark:text-zinc-200 active:bg-fg/10 hover:bg-slate-900/[0.06] hover:text-slate-950 dark:hover:bg-white/[0.14] dark:hover:text-white", )} > <span className="truncate flex-1 min-w-0"> {conversation.title || "Untitled"} {regenMut.isPending && ( <RefreshCw className="inline-block ml-1.5 h-3 w-3 animate-spin text-fg-subtle" /> )} </span> <button onClick={(e) => { e.preventDefault(); e.stopPropagation(); setMenuOpen((v) => !v); }} className="opacity-100 md:opacity-0 md:group-hover:opacity-100 h-7 w-7 grid place-items-center text-fg-subtle transition-opacity" aria-label="Conversation options" > <MoreHorizontal className="h-3.5 w-3.5" /> </button> </Link> {menuOpen && ( <ContextMenu onClose={() => setMenuOpen(false)} items={[ { icon: <Pencil className="h-3.5 w-3.5" />, label: "Rename", onClick: () => { setDraft(conversation.title || ""); setEditing(true); }, }, { icon: <RefreshCw className="h-3.5 w-3.5" />, label: "Auto-rename", onClick: () => regenMut.mutate(), }, ...(styleProfiles.length > 0 ? [ { icon: <Sparkles className="h-3.5 w-3.5" />, label: "Change style", onClick: () => setStylePickerOpen(true), }, ] : []), ...(isProtectedConversation ? [] : [
                ...(isProtectedConversation ? [] : [{ icon: <Trash2 className="h-3.5 w-3.5" />, label: "Delete", onClick: onDelete, danger: true }]),
              ]), ]} /> )} {stylePickerOpen && ( <StylePickerPopover profiles={styleProfiles} currentId={conversation.style_profile_id ?? null} onPick={(id) => { styleMut.mutate(id); setStylePickerOpen(false); }} onClose={() => setStylePickerOpen(false)} /> )} </div> );
} function ContextMenu({ items, onClose,
}: { items: { icon: React.ReactNode; label: string; onClick: () => void; danger?: boolean }[]; onClose: () => void;
}) { const ref = useRef<HTMLDivElement>(null); useEffect(() => { function onDocClick(e: MouseEvent) { if (ref.current && !ref.current.contains(e.target as Node)) onClose(); } function onEsc(e: KeyboardEvent) { if (e.key === "Escape") onClose(); } document.addEventListener("mousedown", onDocClick); document.addEventListener("keydown", onEsc); return () => { document.removeEventListener("mousedown", onDocClick); document.removeEventListener("keydown", onEsc); }; }, [onClose]); return ( <div ref={ref} className="absolute right-1 top-9 z-30 min-w-[140px] glass-strong rounded-lg shadow-lg shadow-black/20 border border-border py-1 overflow-hidden fade-up" role="menu" > {items.map((item, i) => ( <button key={i} onClick={() => { item.onClick(); onClose(); }} className={cn( "w-full flex items-center gap-2 px-3 py-1.5 text-xs text-left transition-colors", item.danger ? "text-fg-soft hover:bg-danger-soft hover:text-red-700 dark:hover:text-red-300" : "text-fg-soft dark:text-zinc-200 hover:bg-slate-900/[0.06] hover:text-slate-950 dark:hover:bg-white/[0.14] dark:hover:text-white", )} > <span className="text-fg-muted">{item.icon}</span> {item.label} </button> ))} </div> );
} function StylePickerPopover({ profiles, currentId, onPick, onClose,
}: { profiles: StyleProfile[]; currentId: string | null; onPick: (id: string | null) => void; onClose: () => void;
}) { const ref = useRef<HTMLDivElement>(null); useEffect(() => { function onDocClick(e: MouseEvent) { if (ref.current && !ref.current.contains(e.target as Node)) onClose(); } function onEsc(e: KeyboardEvent) { if (e.key === "Escape") onClose(); } document.addEventListener("mousedown", onDocClick); document.addEventListener("keydown", onEsc); return () => { document.removeEventListener("mousedown", onDocClick); document.removeEventListener("keydown", onEsc); }; }, [onClose]); return ( <div ref={ref} className="absolute right-1 top-9 z-30 min-w-[200px] glass-strong rounded-lg shadow-lg shadow-black/20 border border-border py-1 overflow-hidden fade-up" role="menu" > <p className="px-3 py-1.5 text-[10px] font-semibold uppercase tracking-wider text-fg-subtle"> Conversation style </p> <StyleOption label="Default" active={currentId === null} onClick={() => onPick(null)} /> {profiles.map((p) => ( <StyleOption key={p.id} label={p.profile_name} active={currentId === p.id} onClick={() => onPick(p.id)} /> ))} </div> );
} function StyleOption({ label, active, onClick,
}: { label: string; active: boolean; onClick: () => void;
}) { return ( <button onClick={onClick} className={cn( "w-full flex items-center justify-between gap-2 px-3 py-1.5 text-xs text-left transition-colors truncate", active ? "bg-accent-soft text-fg font-medium" : "text-fg-soft ", )} > <span className="truncate">{label}</span> {active && <span className="text-[10px] text-accent shrink-0">●</span>} </button> );
} function NewChatStyleDropdown({ profiles, onPick, disabled,
}: { profiles: StyleProfile[]; onPick: (profileId: string | null) => void; disabled?: boolean;
}) { const [open, setOpen] = useState(false); const ref = useRef<HTMLDivElement>(null); useEffect(() => { if (!open) return; function onDocClick(e: MouseEvent) { if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false); } function onEsc(e: KeyboardEvent) { if (e.key === "Escape") setOpen(false); } document.addEventListener("mousedown", onDocClick); document.addEventListener("keydown", onEsc); return () => { document.removeEventListener("mousedown", onDocClick); document.removeEventListener("keydown", onEsc); }; }, [open]); return ( <div ref={ref} className="relative"> <button onClick={() => setOpen((v) => !v)} disabled={disabled} className="h-full px-2.5 rounded-xl bg-accent text-on-accent hover:bg-accent-hover transition-all active:scale-[0.98] disabled:opacity-60" aria-label="Choose chat style" title="Choose chat style" > <ChevronDown className="h-4 w-4" strokeWidth={2.5} /> </button> {open && ( <div className="absolute right-0 top-12 z-30 w-[min(15rem,calc(100vw-3rem))] max-h-80 overflow-y-auto rounded-2xl border border-slate-200/70 bg-white/95 py-2 shadow-2xl shadow-slate-900/15 backdrop-blur-xl fade-up dark:border-white/10 dark:bg-zinc-950/90 dark:shadow-black/30" role="menu" > <div className="px-3 pb-2 pt-1"> <p className="text-[10px] font-semibold uppercase tracking-[0.18em] text-cyan-700 dark:text-cyan-300/80"> Choose chat style </p> <p className="mt-1 text-[11px] leading-4 text-slate-500 dark:text-zinc-400"> Start a new chat with a style profile. </p> </div> <button onClick={() => { onPick(null); setOpen(false); }} className="w-full px-3 py-2 text-left text-sm text-slate-700 transition-colors dark:text-zinc-300 " > Default </button> {profiles.map((p) => ( <button key={p.id} onClick={() => { onPick(p.id); setOpen(false); }} className="w-full truncate px-3 py-2 text-left text-sm text-slate-700 transition-colors dark:text-zinc-300 " > {p.profile_name} </button> ))} </div> )} </div> );
} function NavLink({ href, icon, label, showDot, badge,
}: { href: string; icon: React.ReactNode; label: string; showDot?: boolean; badge?: number;
}) { return ( <Link href={href} className="group w-full flex items-center gap-2.5 rounded-lg px-2.5 py-2 text-sm text-fg-soft dark:text-zinc-200 active:bg-fg/10 transition-colors hover:bg-slate-900/[0.06] hover:text-slate-950 dark:hover:bg-white/[0.14] dark:hover:text-white" > <span className="text-fg-muted dark:text-zinc-300 transition-colors group-hover:text-slate-950 dark:group-hover:text-white">{icon}</span> <span className="flex-1">{label}</span> {badge != null && badge > 0 ? (
        <span className="min-w-5 rounded-full bg-amber-400 px-1.5 py-0.5 text-center text-[10px] font-semibold text-slate-950 shadow-sm shadow-amber-400/20">
          {badge > 99 ? "99+" : badge}
        </span>
      ) : null} {showDot && ( <span className="h-1.5 w-1.5 rounded-full bg-accent shadow-sm shadow-accent/40" aria-label="needs attention" /> )} </Link> );
}
